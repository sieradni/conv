# User Notes

memory roots and neighbors tab breaks ui, making it small and to the left. Notes should auto save. The chat and approval, and expanding does not show the details of the command, which is only shown afterward. Ai response doesn't seem to show anything and does not display ogngoing tokens that are generated. The chat input should stay at the bottom. Move approval to a floating prompt at the top.copy
edit
rerun
delete
branch
only show up for tasks, and not anything else. I am unable to send a message without sending a task first. Also, sending does not clear the chatbox. The agent also seems to still follow the system prompt with tell it to implement math util. test if iesjojwifwefew